services:
  app:
    volumes:
      - .:/var/www/html
      - ./docker/php/php.ini:/usr/local/etc/php/php.ini
    environment:
      APP_ENV: local
      APP_DEBUG: "true"

  nginx:
    volumes:
      - .:/var/www/html
